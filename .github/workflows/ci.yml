name: CI - Seguridad y Calidad FerIA

on:
  push:
    branches: [ workflow ]
  pull_request:
    branches: [ workflow ]

jobs:
  seguridad-y-calidad:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Instalar Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Instalar dependencias
        run: npm install

      # --- SEGURIDAD ---
      - name: Vulnerabilidades en dependencias
        run: npm audit --audit-level=high

      - name: Detectar secrets expuestos
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: main
          head: HEAD

      - name: Análisis OWASP y React security
        uses: returntocorp/semgrep-action@v1
        with:
          config: >-
            p/react
            p/typescript
            p/owasp-top-ten
            p/secrets

      # --- CALIDAD ---
      - name: Buenas prácticas ESLint
        run: npx eslint src/ --ext .ts,.tsx --max-warnings=0

      - name: Verificar TypeScript
        run: npx tsc --noEmit

      # --- BUILD ---
      - name: Build limpio
        run: npm run build

      # --- DOCKER + AWS (activar cuando estés listo para producción) ---
      # - name: Configurar credenciales AWS
      #   uses: aws-actions/configure-aws-credentials@v2
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: us-east-1

      # - name: Login a Amazon ECR
      #   id: login-ecr
      #   uses: aws-actions/amazon-ecr-login@v1

      # - name: Build y push imagen Docker
      #   env:
      #     ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
      #     ECR_REPOSITORY: feria-app
      #     IMAGE_TAG: ${{ github.sha }}
      #   run: |
      #     docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
      #     docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      # - name: Deploy en AWS ECS
      #   run: |
      #     aws ecs update-service \
      #       --cluster feria-cluster \
      #       --service feria-service \
      #       --force-new-deployment